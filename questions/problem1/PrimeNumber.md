1. You have to check whether a given number is prime or not.
2. Take a number `t` as input representing count of numbers to be tested.
3. Take a number `n` as input `t` times.
4. For each input value of `n`, print `prime`, if the number is prime and `not prime` otherwise.